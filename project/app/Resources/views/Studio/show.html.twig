{% extends "::editor.html.twig" %}

{% block title %}
    {{ parent() }}
    {% trans from "studio" %}studio.show{% endtrans %}
{% endblock %}

{% block content %}
    <div class="col-md-9 meta">
        <div class="content">
            <h2>
                {{ entity.name }}
                <a href="{{ path('intent_backend_studio_edit', {'id' : entity.id}) }}" class="glyphicon glyphicon-pencil"></a>
            </h2>
            {{ entity.description|raw }}
        </div>
        <div class="meta">
        </div>
    </div>
{% endblock %}
